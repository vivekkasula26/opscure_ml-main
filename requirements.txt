# Opscure AI Pipeline Dependencies
# CorrelationBundle → AIRecommendation

# Core Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Async HTTP (for Ollama client)
aiohttp>=3.9.0

# Vector Store (Pinecone) - optional
# pinecone-client>=3.0.0
pinecone>=3.0.0

# OpenAI (for embeddings) - optional
openai>=1.12.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
